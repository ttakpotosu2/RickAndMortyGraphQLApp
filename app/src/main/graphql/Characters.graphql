query CharacterList($page: Int) {
    characters (page: $page) {
        results {
            id
            image
            name
            status
            species
            gender
            episode {
                id
            }
        }
    }
}